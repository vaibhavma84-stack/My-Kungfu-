<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/item_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingTop="4dp"
    android:paddingBottom="4dp">

    <!-- FrameLayout, not View: RemoteViews accepts only a fixed list of view
         types and a plain <View> is not on it. One here made every row of the
         list fail to inflate, so the widget showed nothing at all. -->
    <FrameLayout
        android:id="@+id/item_stripe"
        android:layout_width="3dp"
        android:layout_height="26dp"
        android:background="#3A5A78" />

    <TextView
        android:id="@+id/item_mark"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:paddingStart="7dp"
        android:paddingEnd="6dp"
        android:textColor="#B8C4CE"
        android:textSize="12sp"
        android:text="▢" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">
        <TextView
            android:id="@+id/item_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textColor="#F0F4F7"
            android:textSize="13sp"
            android:maxLines="2"
            android:ellipsize="end" />
        <TextView
            android:id="@+id/item_note"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#7FA6C8"
            android:textSize="10sp"
            android:maxLines="1" />
    </LinearLayout>
</LinearLayout>
